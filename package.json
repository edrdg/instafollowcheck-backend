{
  "name": "instafollowcheck-backend",
  "version": "1.0.0",
  "description": "InstaFollowCheck backend API (Puppeteer analysis, WebSocket, SSE). Runs on Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "node scripts/install-chrome.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "puppeteer": "^23.5.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "ws": "^8.18.0"
  }
}